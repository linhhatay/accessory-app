import axios from 'axios';

class ProductModal {
    _products = [];

    constructor() {
        // this._getProducts();
    }

    async _getProducts() {
        // const res = await axios.get('/api/v1/products');
        // console.log(res);
    }
}

export default ProductModal;
